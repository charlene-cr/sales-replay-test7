reviews:
  path_instructions:
    - path: "test/generated-fixtures/pr-10-add-policy-rule-engine.mjs"
      instructions: |
        This generated fixture contains deterministic policy rule engine transaction data. Keep transaction records stable and realistic: IDs must stay unique and sequential for the fixture, `accountId` values should reference existing fixture accounts, `amount` must remain an integer minor-unit value, and `postedOn` must use ISO `YYYY-MM-DD` dates in chronological order. Flag duplicate IDs, skipped ordering, invalid account references, non-integer amounts, or ambiguous date formats.
